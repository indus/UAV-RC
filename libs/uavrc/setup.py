from distutils.core import setup
import setuptools
setup(
    name = 'uavrc',
    packages = ['uavrc'], # this must be the same as the name above
    version = '0.1',
    description = 'A lib for the uavrc.',
    author = 'Martin Israel',
    author_email = 'martin.israel@dlr.de',
    keywords = ['uav-rc','nodejs','message'], # arbitrary keywords
    classifiers = []
)